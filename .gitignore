# Build artifacts — these were previously committed and got bundled into the
# published package by the release workflow.
*.zip
dist/

node_modules/
.DS_Store
Thumbs.db
.vscode/

# Local QA scratch
dev/.*

# Wallpaper sources. The originals are full-resolution PNGs measured in tens of
# megabytes; only the converted `wallpaper/default.webp` is meant to be tracked
# or shipped. Run `python dev/make-wallpaper.py <source>` to regenerate it.
dev/wallpaper-src/

# Local editor and Claude metadata
.claude/
.vscode/
